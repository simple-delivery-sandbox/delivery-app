import { Outlet } from 'react-router-dom';

export const Layout: React.FC = () => {
    return (
        <>
            <header>

            </header>
            <Outlet />
            <footer>

            </footer>
        </>
    )
}