import { Outlet } from 'react-router-dom';

export const UserLayout: React.FC = () => {
    return (
        <>
            <header>

            </header>
            <Outlet />
            <footer>

            </footer>
        </>
    )
}